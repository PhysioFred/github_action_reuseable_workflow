name: Greet Someone

# This workflow runs on push events
on:
  push:
    branches:
      - feature/greeting
      - main

jobs:
  call-greet:
    uses: ./.github/workflows/reusable-greet.yml
    with:
      person-name: Greg